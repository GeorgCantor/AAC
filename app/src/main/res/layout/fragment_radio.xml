<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.georgcantor.aac.view.view.main.MainViewModel" />

        <variable
            name="adapter"
            type="com.georgcantor.aac.view.view.adapter.PosterCircleAdapter" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:padding="4dp"
            app:adapter="@{adapter}"
            app:adapterPosterCircleList="@{viewModel.articles}"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:spanCount="2"
            app:toast="@{viewModel.toastLiveData}"
            tools:listitem="@layout/item_poster_circle" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>